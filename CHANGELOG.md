# 版本记录 / CHANGELOG

## v0.3.1-demo · 2026-09-15（当前 · 交付后实测修复）
**主题：修复“危险源/风险描述”式风险表整表漏读，并归并同义工序节点**

修复（均为 7 个演示包实测复现）
- **P0** 风险表识别过窄 → 表头不含“风险场景”字样时整张风险表被跳过。
  例：起爆药线的「序号｜工房岗位｜危险源｜风险描述｜风险等级｜可能点火源｜现有管控措施｜建议改进措施」表，
  **12 行风险场景全部丢失**（该包 19 条里 0 条来自风险表）。现增加兜底判据：
  表头同时含“风险等级”与“点火源/管控/措施/后果/危险源/风险描述”类列 → 同样按风险场景表处理。
- **P1** 风险表「危险源」列未单独识别 → 现独立取列并写入条目证据（证据新增“危险源：…”）。
- **P1** 同义工序名并存 → 区域/岗位类工艺包会同时出现「合成岗位」（风险表）与「合成（络合沉淀）工序」（工艺流程简述）
  两个指向同一工序的节点。现按「去括号内容 + 去 工序/岗位/区域 等后缀」的**核心名**归并：
  保留文档中先定义者，其余记为节点 `aliases` 别名（条目归属不受影响）。
- **P2** 「定员：A ≤2 人；B ≤1 人；C …」一类**并列叙述行**会把工序名误判到文档很靠前的位置、导致流程图顺序错乱
  → 此类“非工序定义行且并列提及 ≥2 个候选名”的行不再计入“定义位置”。
- **P2** 「全装置 / 全厂 / 通用」等通用单元名不再生成流程节点（条目的 `unit` 归属保留，避免“全装置”伪节点）。

验证（7 个演示工艺包全量回归，同一台机器）
| 归档目录 | 条目 | 主链 | 关键改善 |
|---|---|---|---|
| 01 自动化生产线-硝化棉单基发射药 | 68（不变） | 9（不变） | — |
| 02 常规生产线-硝化棉单基发射药 | 45（不变） | 9（不变） | — |
| 03 双基发射药连续化生产线 | 42（不变） | 13（不变） | — |
| 04 起爆药合成与干燥筛分线 | **19 → 31** | **7 → 8** | 风险表 12 行场景全部纳入；6 组同义岗位名归并 |
| 05 工业雷管装配生产线 | 36（不变） | 12（不变） | — |
| 06 药粒包覆与混同包装线 | 29（不变） | 8（不变） | — |
| 07 成品储存与智能输送线 | 35（不变） | 11（不变） | — |

合计 **274 → 286 条**；严重度 极高 16 / 高 110 / 中 157 / 低 3 / 待定 0。
回归基线同步更新：`成果归档/_批次对比汇总.md`、`成果归档/_系统优化建议（第3批辨识实测）.md`、`交接说明.md`、`快速启动.md`。

代码变更：`app/engine/rules.py`（风险表识别 + 危险源列）、`app/engine/flow.py`（同义归并 + 定义位置 + 通用单元过滤）。
未升版本号：本版为 v0.3.1-demo 内的缺陷修复，接口与前端均无变化。

## v0.3.1-demo · 2026-09-10
**主题：第3批实测（5 个新工艺包）缺陷修复 —— 提升对“非工序命名体系”与“文档结构差异”的兼容性**

修复（均为实测复现，详见 `成果归档/_系统优化建议（第3批辨识实测）.md`）
- **P0** 风险场景表“工序/区域”列名映射过窄 → 补 `区域/工位/岗位/工房/工区/作业区/场所/车间`；
  列名全不命中时回退取“非 序号/场景/等级/点火源/管控/建议 的首列”；占位符由“（风险场景表）”改为“（未标注单元）”。
- **P0** 流程图主链退化为单个占位节点 → `flow.py` 新增候选过滤（拒收“（…）”与含 与/及/和/等/、 的伪节点）
  与“定义位置排序”（优先“××工序：”列表项/标题）→ 修复主链顺序错乱与伪节点“包覆与石墨工序”。
- **P0** 前端 `setView` 未判空 → 未打开报告时点页签报 `Cannot read properties of null (reading 'flow')`，已加守卫。
- **P1** 正文（工艺流程简述）里的工序未进主链 → 新增 `××工序：/工位/岗位：` 列表项扫描并入候选（主链上限 16）。
- **P1** “未配置机器人”被误报为机器人风险线索 → 机器人叙述扫描加否定语境过滤（`ROBOT_NEG_RE`）；
  `_guess_process` 不再返回“（机器人）/（机械手）”伪单元名。
- **P2** 参考文件/机器人台账为空时报告章节跳号 → 7 章编号固定，空章节给出补齐建议的说明文案（HTML + Word）。
- **P2** 物料条目严重度大量“待定” → SDS 无危险性标签时给规则建议值并标注“（严重度为规则建议值，待人工确认）”。
- **P2** “单元/章节”与“工序单元”口径不一 → 新增 `process_units`（与流程图节点一致），概览表/单元卡/报告 1.4 节分开展示。
- **P2** 场景表“建议改进措施”列被丢弃 → 独立识别并写入条目的“建议措施”。
- **P3** SDS 标签兼容：增加 `危险分类/危险类别/燃爆特性`（适配不同工艺包写法）。
- **P3** `AUX_KW` 的 `储` → `储罐`，避免“自动仓储区域”被误判为辅助节点。

验证
- 第 3 批 5 个新工艺包（03 双基发射药连续化 / 04 起爆药合成 / 05 工业雷管装配 / 06 药粒包覆包装 / 07 成品储存与智能输送）
- 修复前：07 主链 1 节点、04 误报机器人 2 条；修复后：07 主链恢复为区域链、04 机器人误报清零。

## v0.3.0-demo · 2026-09-10
**主题：自动化（机器人）+ 设计依据 + 可阅读报告**

新增
- 自动化/机器人辨识：解析“机器人台账表”→ 每台机器人 1 条 `自动化·机器人` 条目；
  新增“🤖 机器人”页（一条目一页）：负责工序 / 工序内容 / 减少操作人数 / 该工序风险 /
  机器人可能新增风险 / 更改后避免的风险 / 防爆与安全要求 / **选型参数、选型说明、选型图片** / 关联条目。
- “📚 参考文件”页：自动抽取国标/行标/规范/参考书并分类去重（附来源行号）。
- “📄 报告预览”页：内嵌可阅读 HTML 报告，可 **下载 Word**、新窗口打开、打印为 PDF。
- 概览增强：机器人摘要卡与 KPI、参考文件摘要卡、热力图新增“自动化·机器人”列、
  修复丢失的“识别到的单元/章节”区块。
- 工序流程图节点显示 🤖 机器人（-N 人）；故障树新增“⚠ 上游传递风险”（工序间传播）。
- 故障树/流程图/机器人/参考文件 与“条目列表·条目总表”互相跳转。

修复
- “概览”与“条目列表”进入时显示同一内容 → “条目列表”改为**条目总表**（可点行进单页）。
- 分布条/排序条因 `<span>` 内联导致宽度无效、看不到颜色 → `display:block` + `min-width`。
- 热力图与排序改为红色系渐变色带。
- 工艺流程图由横向长条改为**蛇形折行**，消除左右拖动。
- 导出 JSON / 下载 Word 因中文文件名写进 HTTP 头报 500 → RFC 5987 编码。
- `.docx` 不嵌入 SVG → 优先使用同名 `.png`（由 SVG 渲染）嵌入 Word。

## v0.2.0-demo · 2026-09-10
- 四视图报告：概览（风险可视化）/ 工艺流程图（交互）/ 故障树 FTA / 条目列表（一条目一页）。
- 本地规则引擎 + 可选大模型（OpenAI 兼容）增强；上传 .md/.docx、粘贴文本、演示样例。
- 首个演示工艺包（硝化棉单基发射药）。

## v0.1.0-demo · 2026-09-10
- 项目骨架：FastAPI 后端 + 原生前端；文档摄取（.md/.docx → 规范化文本）、
  规则辨识（风险场景表 / SDS / 联锁 / 点火源 / 作业）、报告 JSON 与 REST 接口。
